/**
 * Enum for classify response code from MyTsel service type
 */

export enum MyTselResponseStatusCodeEnum {
  SUCCESS = 200,
  BAD_REQUEST = 400,
  INTERNAL_ERROR = 500,
  REQUEST_TIMEOUT = 408,
}
