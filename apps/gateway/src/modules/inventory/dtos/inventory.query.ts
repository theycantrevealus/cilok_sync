export class InventoryQuery {
  locale: string;
  multi_locale_flag: boolean;
  search_term: string;
  limit: number;
  skip: number;
  filter: object;
  sort: object;
  projection: object;
  addon: object;
  realm_id: string;
  branch_id: string;
  merchant_id: string;
}

